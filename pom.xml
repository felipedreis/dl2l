<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <artifactId>l2l</artifactId>
    <groupId>br.cefetmg.lsi</groupId>
    <version>2.0.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>L2L Core Module</name>
    <description>This project is the core of the L2L architecture (Learn to Live, Live to Learn)</description>
    <url>http://www.lsi.cefetmg.br/</url>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <jdk.version>23</jdk.version>
        <scala.version>2.11</scala.version>
        <arrow.version>18.3.0</arrow.version>

    </properties>

    <dependencies>

        <dependency>
            <groupId>commons-cli</groupId>
            <artifactId>commons-cli</artifactId>
            <version>1.2</version>
        </dependency>

        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-math3</artifactId>
            <version>3.2</version>
        </dependency>

        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-collections4</artifactId>
            <version>4.5.0-M1</version>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.9.2</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <version>5.9.2</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.google.code.tempus-fugit</groupId>
            <artifactId>tempus-fugit</artifactId>
            <version>1.1</version>
            <exclusions>
                <exclusion>
                    <groupId>org.hamcrest</groupId>
                    <artifactId>hamcrest-core</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>junit</groupId>
                    <artifactId>junit</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- ArrowIpcBackend (see PersistenceBackend) - the sole persistence write path, no
             SQL/DB layer. Off-heap Arrow IPC stream writer - see docs/plans/arrow-ipc-write-path.md
             for why this replaced the earlier direct-Parquet ParquetBackend (still in git history).
             arrow-memory-unsafe (not -netty) is the allocator backend: no extra netty jars in the
             fat jar (Akka 2.5 already ships netty 3, a different package but still a duplicate-class
             shading risk to avoid) and per-allocation overhead is irrelevant to this reuse-heavy
             pattern (buffers allocated once per table, hot path is setSafe into existing memory).
             Requires the JVM flag add-opens=java.base/java.nio=ALL-UNNAMED (see
             scripts/run-dl2l.sh) - arrow-memory-core's MemoryUtil needs it on JDK 16+; target is
             ALL-UNNAMED only since this project has no module-info.java (classpath, not
             module-path). -->
        <dependency>
            <groupId>org.apache.arrow</groupId>
            <artifactId>arrow-vector</artifactId>
            <version>${arrow.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.arrow</groupId>
            <artifactId>arrow-memory-core</artifactId>
            <version>${arrow.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.arrow</groupId>
            <artifactId>arrow-memory-unsafe</artifactId>
            <version>${arrow.version}</version>
        </dependency>
        <!-- Only exercised when DL2L_ARROW_COMPRESSION=lz4|zstd (default: none/off - see
             ArrowIpcBackend). Declared unconditionally since Maven dependencies aren't
             runtime-conditional; the LZ4/ZSTD codepath is otherwise dead code. -->
        <dependency>
            <groupId>org.apache.arrow</groupId>
            <artifactId>arrow-compression</artifactId>
            <version>${arrow.version}</version>
        </dependency>

        <dependency>
            <groupId>org.apache.directory.studio</groupId>
            <artifactId>org.apache.commons.io</artifactId>
            <version>2.4</version>
        </dependency>

        <dependency>
            <groupId>org.scala-lang</groupId>
            <artifactId>scala-library</artifactId>
            <version>2.11.7</version>
        </dependency>

        <dependency>
            <groupId>com.typesafe.akka</groupId>
            <artifactId>akka-actor_${scala.version}</artifactId>
            <version>2.5.32</version>
        </dependency>

        <dependency>
            <groupId>com.typesafe.akka</groupId>
            <artifactId>akka-remote_${scala.version}</artifactId>
            <version>2.5.32</version>
        </dependency>

        <dependency>
            <groupId>com.typesafe.akka</groupId>
            <artifactId>akka-cluster_${scala.version}</artifactId>
            <version>2.5.32</version>
        </dependency>

        <dependency>
            <groupId>com.typesafe.akka</groupId>
            <artifactId>akka-cluster-sharding_${scala.version}</artifactId>
            <version>2.5.32</version>
        </dependency>

        <dependency>
            <groupId>com.typesafe.akka</groupId>
            <artifactId>akka-stream_${scala.version}</artifactId>
            <version>2.5.32</version>
        </dependency>
        <dependency>
            <groupId>com.typesafe.akka</groupId>
            <artifactId>akka-http_${scala.version}</artifactId>
            <version>10.1.15</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>2.15.2</version>
        </dependency>

        <!-- Micrometer: in-process metrics, scraped by Prometheus -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-core</artifactId>
            <version>1.12.5</version>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
            <version>1.12.5</version>
        </dependency>

        <!-- DJL: load species TorchScript models once per JVM node -->
        <dependency>
            <groupId>ai.djl</groupId>
            <artifactId>api</artifactId>
            <version>0.26.0</version>
        </dependency>
        <dependency>
            <groupId>ai.djl.pytorch</groupId>
            <artifactId>pytorch-engine</artifactId>
            <version>0.26.0</version>
        </dependency>
        <!--
          Platform-specific PyTorch native library, bundled so the fat jar
          never needs to reach publish.djl.ai at runtime to auto-download it
          — CONFIRMED LIVE this download is unreachable from CCAD compute
          nodes (no internet access there), which silently crashed every
          Holder's preStart() (MLServiceExtension -> DJL PtEngine init) on
          CCAD specifically. Every other environment this ran in (local Mac,
          Pi cluster) has internet, so a classifier mismatch here was masked
          by DJL's runtime fallback download and never surfaced before.
          linux-x86_64 matches what docker/Dockerfile actually builds/runs
          (the image used everywhere except local Mac dev) — for local dev
          on macOS ARM64, DJL auto-downloads the correct osx-aarch64 native
          lib on first run instead (Mac dev always has internet).
        -->
        <dependency>
            <groupId>ai.djl.pytorch</groupId>
            <artifactId>pytorch-native-cpu</artifactId>
            <classifier>linux-x86_64</classifier>
            <version>2.1.1</version>
            <scope>runtime</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <source>23</source>
                    <target>23</target>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
                <configuration>
                    <!-- ArrowIpcBackendTest (and anything else touching arrow-memory-unsafe's
                         RootAllocator) needs the same add-opens flag the JVM launch script
                         grants in production - see scripts/run-dl2l.sh and ArrowIpcBackend's
                         javadoc. -->
                    <argLine>--add-opens=java.base/java.nio=ALL-UNNAMED</argLine>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <!-- Bumped proactively from 2.4.1 for Arrow 18.x (see docs/plans/arrow-ipc-write-path.md,
                     W2): Arrow's jars carry module-info.class and multi-release sections that
                     pre-2015 2.4.1 mishandles. Not gated behind a failure - the fallback
                     (arrow-memory-netty) needing this too is unlikely enough not to wait for. -->
                <version>3.6.2</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <shadedArtifactAttached>true</shadedArtifactAttached>
                    <shadedClassifierName>allinone</shadedClassifierName>
                    <artifactSet>
                        <includes>
                            <include>*:*</include>
                        </includes>
                    </artifactSet>
                    <filters>
                        <filter>
                            <artifact>*:*</artifact>
                            <excludes>
                                <!-- module-info.class from Arrow's (and other) JPMS-aware jars -
                                     shading multiple modules' descriptors into one jar is invalid
                                     and unnecessary (the fat jar is a plain classpath jar, not a
                                     module). -->
                                <exclude>module-info.class</exclude>
                                <exclude>META-INF/versions/*/module-info.class</exclude>
                            </excludes>
                        </filter>
                    </filters>
                    <transformers>
                        <transformer
                                implementation="org.apache.maven.plugins.shade.resource.AppendingTransformer">
                            <resource>reference.conf</resource>
                        </transformer>
                        <transformer
                                implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                        <transformer
                                implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                            <manifestEntries>
                                <Main-Class>br.cefetmg.lsi.l2l.Main</Main-Class>
                            </manifestEntries>
                        </transformer>
                    </transformers>

                    <finalName>${project.build.finalName}-wd</finalName>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <configuration>
                    <archive>
                        <manifest>
                            <addClasspath>true</addClasspath>
                            <mainClass>br.cefetmg.lsi.l2l.Main</mainClass>
                            <classpathPrefix>dependency-jars</classpathPrefix>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>