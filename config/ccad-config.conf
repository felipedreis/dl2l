object-dispatcher {
  type = Dispatcher
  executor = "fork-join-executor"

  fork-join-executor {
    parallelism-min = 2
    parallelism-factor = 2.0
    parallelism-max = 20
  }
  throughput = 10
}

bd-dispatcher {
  type = PinnedDispatcher
  mailbox-type = "br.cefetmg.lsi.l2l.common.ComponentMailbox"
  executor = "thread-pool-executor"
}

wm-dispatcher {
  type = PinnedDispatcher
  executor = "thread-pool-executor"
  thread-pool-executor {
    fixed-pool-size = 1
  }
}

collision-dispatcher {
  mailbox-type = "br.cefetmg.lsi.l2l.cluster.CollisionDetectorPriorityMailbox"
}

component-dispatcher {
  mailbox-type = "br.cefetmg.lsi.l2l.common.ComponentMailbox"
}

akka.actor.deployment {
  "/holder/object-*" {
    dispatcher = object-dispatcher
  }
}


akka {
  loglevel = "DEBUG"

  actor {
    #provider = "akka.cluster.ClusterActorRefProvider"
    provider = "cluster"
    guardian-supervisor-strategy = "akka.actor.StoppingSupervisorStrategy"
  }
  remote {
    artery {
      enabled = false  # Since you're using classic remoting
    }
    netty.tcp {
      hostname = ${HOST}
      port = ${PORT}
      bind-hostname = ${HOST} #0.0.0.0 # Allow external connections
      bind-port = ${PORT}
    }
    log-remote-lifecycle-events = on
    use-passive-connections = off
  }

  cluster {
    # CCAD's Singularity instances share the allocated node's network
    # namespace (no per-container hostnames/DNS like Docker's bridge
    # network), so every role uses HOST=localhost with a distinct PORT
    # instead of a container-name seed address — manager always claims
    # 2551 first (see roles/trial_runner_ccad/templates/run_trial.sh.j2).
    seed-nodes = [
      "akka.tcp://l2l@localhost:2551",
    ]
    min-nr-of-members = 2
  }
  version = 2.5.32
}
