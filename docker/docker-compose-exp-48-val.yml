version: "3.1"
# Validation compose for EXP-48 (A and B).
# Uses the same world as exp-p6-1 (3 creatures, 90 RED + 90 GREEN apples, Mode-2 ON).
# Holder and CollisionDetector share one JVM so TypedActor.getActorRefFor() works.
services:
  dl2l-db:
    image: postgres
    container_name: db
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: l2l
      POSTGRES_INITDB_ARGS: "--auth-host=md5"
    ports:
      - "5432:5432"
    networks:
      - dl2l-network
    volumes:
      - source: ../config/init-db.sql
        target: /docker-entrypoint-initdb.d/init-db.sql
        read_only: true
        type: bind

  dl2l-manager:
    image: dl2l
    depends_on:
      - dl2l-db
    environment:
      ROLE: "manager,idProvider"
      SIMULATION: "simulations/exp_p6_1_mode2_selection.conf"
      HOST: "dl2l-manager"
      PORT: 2551
    networks:
      - dl2l-network
    healthcheck:
      test: [ "CMD", "nc", "-z", "dl2l-manager", "2551" ]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - source: ../config/docker-config.conf
        target: /config/docker-config.conf
        read_only: true
        type: bind

  dl2l-holder:
    image: dl2l
    depends_on:
      dl2l-manager:
        condition: service_healthy
    environment:
      ROLE: "holder,collisionDetector"
      SIMULATION: "simulations/exp_p6_1_mode2_selection.conf"
      HOST: "dl2l-holder"
      PORT: "2551"
    networks:
      - dl2l-network
    volumes:
      - source: ../config/docker-config.conf
        target: /config/docker-config.conf
        read_only: true
        type: bind

networks:
  dl2l-network:
    driver: bridge
